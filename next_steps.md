# Next steps

- [ ] Add GitHub Actions workflow for publishing to PyPI on tag push
  Create `.github/workflows/publish.yml` that triggers on `v*` tag pushes. Use `astral-sh/setup-uv@v5` (matching the existing `ci.yml` pattern), build with `uv build`, and publish with `uv publish` or `pypa/gh-action-pypi-publish`. The workflow needs a PyPI API token stored as a GitHub Actions secret (e.g. `PYPI_API_TOKEN`). Reference `.github/workflows/ci.yml` for the existing action structure.

- [ ] Add `CHANGELOG.md`
  Create a `CHANGELOG.md` in the repo root following [Keep a Changelog](https://keepachangelog.com) format. Start with an `## [Unreleased]` section and a `## [0.1.0]` section summarizing the initial feature set: CLI (`nbafigs/cli.py`) with `player` and `team` subcommands, Python API (`player_shot_chart()` and `team_shot_chart()` in `nbafigs/__init__.py`), normal and hex shot chart modes, zone grouping via `shots_grouper()`, and retry/rate-limiting via `sources/nba_api_call()`.

- [ ] Verify `nbafigs` name availability on PyPI
  Go to `https://pypi.org/project/nbafigs/` and confirm the name is not taken. If it is, pick an alternative (e.g. `nba-figures`) and update the `name` field in `pyproject.toml` as well as the `[project.scripts]` entry and any references in `README.md`. Also consider registering the name by doing a test publish to TestPyPI (`https://test.pypi.org`) before the real release.

- [ ] Tag `v0.1.0` and publish
  After the publish workflow and changelog are in place: merge the current branch (`eth-347/refactor`) to `main`, then `git tag v0.1.0` and `git push origin v0.1.0`. This should trigger the publish workflow. Verify the package appears on PyPI and is installable via `pip install nbafigs`. Make sure `pyproject.toml` has the correct `version = "0.1.0"`, `readme`, `license`, and any relevant `[project.urls]` metadata before tagging.

- [ ] Natural language mode: parse prompts via LLM into shot chart parameters
  Add a new CLI command (e.g. `nbafigs ask "Show me Luka's 3-pointers last season"`) that sends the user's natural language prompt to an LLM (Claude API via the `anthropic` SDK) and maps the response to the existing `player_shot_chart()` / `team_shot_chart()` parameters. The LLM should output structured JSON matching the CLI options defined in `nbafigs/cli.py` (player name, seasons, kind, scale, limiters like `OpponentTeam`, `DateFrom`, etc.). Add the `anthropic` package as an optional dependency in `pyproject.toml` under `[project.optional-dependencies]` (e.g. `nlp = ["anthropic>=0.40"]`).

- [ ] Animated shot charts: port `get_ani_shot_chart` from old `nba_player.py`
  The old `nba_player.py` (visible via `git show HEAD:nba_player.py`, lines 412-634) has a `get_ani_shot_chart()` method that uses `matplotlib.animation.FuncAnimation` to produce a GIF cycling through hex shot charts across multiple seasons. Port this into the new structure: add an `animate_shot_chart()` function in `nbafigs/viz/chart.py` that accepts a list of per-season DataFrames (from `shots_grouper()`) and chart params, builds frames using `make_shot_fig()` and the existing hex rendering logic, and returns a `FuncAnimation`. Update the old code to use `matplotlib.colormaps['RdYlBu_r']` instead of the deprecated `cm.get_cmap()`, use `importlib.resources` for the texture image, and use `matplotlib.path.Path` / `matplotlib.patches.PathPatch` (not `mpatches`). Expose it in `NBAPlayer.get_animated_shot_chart()` in `nbafigs/core/player.py` and add a `--animate` flag to the `player` CLI command. Add `pillow` as a dependency for GIF writing.

- [ ] Shot distribution charts: complete `make_shot_dist` histogram
  The old `nba_methods.py` (visible via `git show HEAD:nba_methods.py`, lines 386-430) has a partially implemented `make_shot_dist()` that only handles the `color='make_miss'` stacked histogram case — the `shot_type`, `PCT_DIFF`, `P_PPS`, `D_PPS`, and `team` color modes are stubs (`print('')`). Port the working `make_miss` mode into a new `make_shot_dist()` function in `nbafigs/viz/chart.py`, then implement the remaining color modes: `shot_type` should stack by 2PT/3PT, and the metric-based modes (`PCT_DIFF`, `P_PPS`, `D_PPS`) should color each distance bin by the average metric value using a colormap (e.g. `RdYlBu_r`). Wire it up via `NBAPlayer.get_shot_dist()` in `nbafigs/core/player.py` and expose a `player_shot_dist()` function in `nbafigs/__init__.py`. Add a `dist` CLI subcommand in `nbafigs/cli.py`.
